import * as actionTypes from "../actions";

const initialState = {

}

export const musicReducer = (state = initialState, action) => {
    switch(action.type) {
        
        default:
            return state;
    }
}