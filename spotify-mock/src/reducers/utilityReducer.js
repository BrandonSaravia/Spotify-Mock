import * as actionTypes from "../actions";

const initialState = {

}

export const utilityReducer = (state = initialState, action) => {
    switch(action.type) {
        
        default:
            return state;
    }
}